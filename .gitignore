# Dependencies
node_modules/

# Build output
dist/

# Environment files (contain secrets)
.env
.env.local
.env.production
.env.e2e

# Test database
*.db
*.db-wal
*.db-shm

# Prisma generated (regenerated via `prisma generate`)
# Keep prisma/schema.prisma but ignore generated client artifacts
# node_modules/@prisma is already covered above

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Coverage
coverage/

# Oracle bridge local runtime data (state/backups written at runtime,
# never checked in — the whole point is these are recoverable from a
# real backup location, not from git)
oracle-bridge/data/
